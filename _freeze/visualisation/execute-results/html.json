{
  "hash": "8d9bac72005dff89401bdb1fe9bfdd72",
  "result": {
    "engine": "knitr",
    "markdown": "---\nauthor: Quinn Asena\n---\n\n# Visualising pollen data {#sec-visualisation}\n\n## Background {#sec-visualisationbackground}\n\n<!-- NOTE (Claude): this section still needs the real Background prose. The shape it wants is\n     probably: what a pollen diagram is and why it is drawn the way it is (time vertical, oldest\n     at the bottom, one panel per taxon), then the two honesty problems the chapter deals with,\n     which are (a) proportions hide the count they came from and (b) an area or line between two\n     samples is an interpolation the data do not support. -->\n\nOne of the most common methods of plotting palaeoecological data (such as pollen, but often including diatoms, isotopes, and other palaeoecological data) is to create stratigraphic plots. Palaeodata are regularly visualised with age (usually the output of an age-depth model) on the y-axis, with the youngest age at the top, to mirror a core-type sample. Each taxon is plotted in sequence horizontally, in order of abundance. Taxonomic data, such as pollen or diatoms, are also usually converted to relative abundances before plotting. While the relative abundances do help visualise the proportion of each taxon through time (and absolute abundances are unavailable), it also means that the plots can be misleading. For example, if all taxa were to decline equally, then the figure would not change at all! Or the rise of a taxon could be due to the decline of others. Of course, this is not the only method of visualising palaeoecological data, and other layouts can be equally good; however, we will demonstrate the most common convention in this chapter. We will do so using the `ggplot2` package and the `rioja` package.\n\n\n## Getting started\n\n::: {.callout-note}\n# Packages required for this section\n\n\n::: {.cell}\n\n```{.r .cell-code}\nif (!require(\"pacman\")) install.packages(\"pacman\", repos = \"https://cran.r-project.org\")\npacman::p_load(dplyr, tidyr, ggplot2, scales, stringr, mgcv, rioja, neotoma2, marquee)\n```\n:::\n\n:::\n\n::: {.callout-tip}\n# Respawn code\n\nThis is the same Devil's Lake download used in @sec-age-depth and @sec-dissimilarity, filtered\nto `datasettype == \"pollen\"`, harmonised the same way, and converted to proportions.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nif (!file.exists(\"./data/devils_dl.rds\")) {\n  devils_dl <- get_sites(siteid = 666) %>%\n    get_datasets(all_data = TRUE) %>%\n    neotoma2::filter(datasettype == \"pollen\") %>%\n    get_downloads()\n  saveRDS(devils_dl, \"./data/devils_dl.rds\")\n} else {\n  devils_dl <- readRDS(\"./data/devils_dl.rds\")\n}\n```\n:::\n\n:::\n\n\nMost stratigraphic plots you will see have taxa converted to relative abundances. The following code converts observations from counts (NISP) to proportions. In this section we will not go into detail on the `samples()` function or the conversion to proportions as this is covered in @sec-neotoma2 and @sec-dissimilarity.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndevils_samples <- samples(devils_dl) %>%\n  dplyr::filter(ecologicalgroup %in% c(\"UPHE\", \"TRSH\"),\n                elementtype == \"pollen\",\n                units == \"NISP\")\n\ndevils_harm_prop <- devils_samples %>%\n  mutate(variablename = replace(variablename,\n                                stringr::str_detect(variablename, \"Pinus.*\"),\n                                \"Pinus\")) %>%\n  group_by(siteid, sitename, sampleid, variablename, units,\n           age, agetype, depth, datasetid, long, lat) %>%\n  summarise(value = sum(value), .groups = \"drop\") %>%\n  group_by(age) %>%\n  mutate(pollencount = sum(value, na.rm = TRUE),\n         prop        = value / pollencount) %>%\n  ungroup() %>%\n  arrange(desc(age))\n```\n:::\n\n\n\n\n## Stratigraphic diagrams\n\nIn most publication-ready figures, you would plot as many taxa and functional groups as possible. For this demonstration we will plot the 6 most abundant species so that they fit more easily on the rendered page. We will do this simply by summing the taxa through time and picking the top 6:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndevils_top_taxa <- devils_harm_prop %>%\n  group_by(siteid, sitename, variablename) %>%\n  summarise(total = sum(value), .groups = \"drop\") %>%\n  arrange(desc(total))\n```\n:::\n\n\nOf course, this is an over-simplification since ecologically important does not necessarily also mean most abundant! It serves the purpose of having something to plot.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfocal <- devils_top_taxa$variablename[1:6]\n# Filter to include only top 6 taxa\ndevils_focal <- devils_harm_prop %>%\n  dplyr::filter(variablename %in% focal)\n```\n:::\n\n\n### Building a stratigraphic plot with `ggplot2`\n\nLet's start with the simplest thing that works: proportion against age, one panel per taxon.\n`coord_flip()` puts age on the vertical axis and `scale_x_reverse()` makes age run downwards, so\nthe oldest sample sits at the bottom. `facet_wrap()` splits each taxon (`variablename`) into one panel per taxon. We will incrementally build up from a basic plot to a fleshed-out example with correct details. If you are unfamiliar with ggplot, try commenting out or removing lines systematically, rerunning the code each time to see the difference.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(devils_focal, aes(x = age, y = prop)) +\n  geom_area(fill = \"grey25\") +\n  scale_x_reverse() +\n  scale_y_continuous() +\n  labs(x = \"Age (cal years BP)\", y = \"Pollen proportions\") +\n  coord_flip() + # confusingly age is the x label because of coord_flip()\n  facet_wrap(~variablename, nrow = 1) + # makes each taxa its own panel\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![A first pollen diagram for Devil's Lake. Incomplete, but something to build on.](visualisation_files/figure-html/fig-strat-first-1.png){#fig-strat-first width=672}\n:::\n:::\n\n\nNow let's start improving on this base plot @fig-strat-first! Using a filled area is common; however,  the fill implies a continuous record and the core was sampled at discrete intervals. Everything between two samples is drawn by the plotting function, not measured. Adding a segment\nat each sampled age shows where the observations actually are. The density of observations through time is very important, and it is a common mistake not to include them in stratigraphic plots! Currently, the panels are also in alphabetical order by default. Ordering by abundance is tidier and conveys relative abundance along the horizontal alignment of panels. To do this `ggplot` likes to have the taxa as factor data, so let's convert the taxa names to factor data:\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# convert taxa to factor, see <fct> now appears in the\n# variablename column if you print the tibble?\ndevils_focal <- devils_focal %>%\n  mutate(variablename = factor(variablename, levels = focal))\n```\n:::\n\n\nNow rebuild the plot with `geom_segment()` to add the sampling locations, and rebuild with the `variablename` as a factor:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(devils_focal, aes(x = age, y = prop)) +\n  geom_area(fill = \"grey25\") +\n  geom_segment(aes(x = age, xend = age, y = 0, yend = prop),\n               colour = \"grey40\", linewidth = 0.3) +\n  scale_x_reverse() +\n  scale_y_continuous() +\n  labs(x = \"Age (cal years BP)\", y = \"Pollen proportions\") +\n  coord_flip() + # confusingly age is the x label because of coord_flip()\n  facet_wrap(~variablename, nrow = 1) + # makes each taxa its own panel\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![The same diagram with a segment marking every sampled level, and the panels ordered by total abundance rather than alphabetically.](visualisation_files/figure-html/fig-strat-second-1.png){#fig-strat-second width=672}\n:::\n:::\n\n\nNow we're getting somewhere!\n\n\n#### Formatting taxon names\n\nBy convention genus and species names are italic, the genus is capitalised and the species is not (e.g., *Quercus alba*). The `marquee` package renders markdown inside `ggplot2` theme elements, so a label is written the way it would be written using [markdown syntax](https://www.markdownguide.org/basic-syntax/#emphasis). Markdown is a featherweight plain text format that is super easy to use and commonplace in programming. There are many many ways of achieving the same aim in `ggplot`, I find markdown elements the simplest.\n\nLet's create some labels for `ggplot`. Since there are only 6 taxa, we can easily enough create matching pairs manually:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntaxon_names <- c(\n  Quercus  = \"*Quercus* spp.\",\n  Pinus    = \"*Pinus* spp.\",\n  Picea    = \"*Picea* spp.\",\n  Ulmus    = \"*Ulmus* spp.\",\n  Fraxinus = \"*Fraxinus* spp.\",\n  Betula   = \"*Betula* spp.\"\n)\n\n# raise an error if anything changes and taxon names do not match.\nstopifnot(all(focal %in% names(taxon_names)))\n```\n:::\n\n\nAlternatively, we can take a simple programmatic approach:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmarkdown_names <- paste0(\"*\", focal, \"* spp.\")\ntaxon_names_prog <- setNames(markdown_names, focal)\nstopifnot(all(focal %in% names(taxon_names_prog)))\n```\n:::\n\n\nFor more complex lists of species names, you can take a rule-based approach with lots of regular expressions (regex) to find and match patterns like `undiff.`, `-type`, and `subg`. Pattern matching text got much easier with the help of artificial intelligence, so generating labels is not a hard task now (it used to be challenging to safely/correctly match all the tricky taxon names as well as isotopes with the correct notation!). \n\nRemember that the taxa are split into facets? Thus, it is the facet strip labels that need to be modified. `as_labeller()` puts the formatted strings into the facet strips, and `element_marquee()` tells `ggplot2` to render the markdown rather than print it. Both are needed: the labeller alone would\nshow the asterisks. Now the same plot with the segments, the ordering and the names:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(devils_focal, aes(x = age, y = prop)) +\n  geom_area(fill = \"grey25\") +\n  geom_segment(aes(x = age, xend = age, y = 0, yend = prop),\n               colour = \"grey40\", linewidth = 0.3) +\n  scale_x_reverse(name = \"Age (cal years BP)\", labels = label_comma()) +\n  scale_y_continuous(name = \"Proportion of the pollen sum\",\n                     labels = label_percent(), n.breaks = 3) +\n  coord_flip() +\n  facet_wrap(~variablename, nrow = 1,\n             labeller = as_labeller(taxon_names)) +\n  theme_minimal() +\n  theme(strip.text        = element_marquee(),\n        panel.spacing.x   = unit(0.9, \"lines\"),\n        panel.grid.minor  = element_blank(),\n        axis.text.x       = element_text(size = 7))\n```\n\n::: {.cell-output-display}\n![Devil's Lake pollen diagram. Each horizontal segment is a sampled level, so the width of the shaded area between segments is interpolation rather than data. Panels are ordered by the total abundance of the taxa.](visualisation_files/figure-html/fig-strat-devils-1.png){#fig-strat-devils width=672}\n:::\n:::\n\n\n\nNotice that we also included some optional cosmetic changes like percent on the x-axis using `label_percent()` and `label_comma()` to the ages on the y-axis.\n\n::: {#exr-freeaxis}\n1. `facet_wrap` includes optional arguments `\"fixed\"`, `\"free\"`, `\"free_x\"`, and `\"free_y\"`. Experiment with the alternative arguments to `facet_wrap` and discuss the consequences of how proportions are visualised.\n:::\n\n::: {.callout-note}\n# Assumptions of stratigraphic plots of taxa\n\n- When plotting relative abundances, that the taxa are _not_ independent of each other. As noted in @sec-visualisationbackground, a perceived increase in one taxa may be driven by the decrease in other species.\n\n- Chronological uncertainties are not visually accounted for. @sec-age-depth covers how important the age-depth uncertainties can be and that only considering the median density can lead to incorrect conclusions. Common errors in palaeoecology are to: (i) infer the timing of ecological events from stratigraphic diagrams that do not account for chronological uncertainty; and (ii) plot data from multiple cores against a single chronology to infer the synchroneity of events across space without due consideration of the compounded chronological uncertainties. Although these considerations are important to visualisation and interpretation, we will not attempt to tackle such big issues in this chapter. If you are interested, here are a few papers:\n  - @blois2011\n  - @parnell2008\n  - @blaauw2007\n  - @blaauw2020\n\n:::\n\n\n\n\n### The same diagram with `rioja`\n\n`rioja` is built for this and does in one call what the section above does in several, including\nthe CONISS constrained cluster analysis that splits the record into zones. It wants a wide\nmatrix, samples in rows and taxa in columns.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndevils_wide <- devils_harm_prop %>%\n  dplyr::select(age, variablename, prop) %>%\n  dplyr::filter(variablename %in% focal) %>%\n  tidyr::pivot_wider(id_cols = age,\n                     names_from  = variablename,\n                     values_from = prop,\n                     values_fill = 0)\n\ndim(devils_wide)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndevils_clust <- rioja::chclust(dist(sqrt(devils_wide[, -1])), method = \"coniss\")\n\nstrat <- rioja::strat.plot(as.data.frame(devils_wide[, -1] * 100),\n                           yvar   = devils_wide$age,\n                           title  = devils_samples$sitename[1],\n                           ylabel = \"Age (cal years BP)\",\n                           y.rev  = TRUE,\n                           clust  = devils_clust,\n                           wa.order = \"topleft\",\n                           scale.percent = TRUE)\n\nrioja::addClustZone(strat, devils_clust, 4, col = \"red\")\n```\n\n::: {.cell-output-display}\n![The same six taxa drawn by `rioja::strat.plot()`, with a CONISS dendrogram on the right and four zones marked in red. `wa.order` sorts the taxa by weighted average age, so the panel order differs from the previous figure.](visualisation_files/figure-html/fig-strat-rioja-1.png){#fig-strat-rioja width=672}\n:::\n:::\n\n\n<!-- NOTE (Claude): the object is called `strat` rather than `plot`, which is what the old draft\n     used. Assigning to `plot` masks base R's plot() for the rest of the session. -->\n\n<!-- NOTE (Claude): CONISS and the distance measure behind it are @sec-dissimilarity's subject,\n     and this chapter currently comes first. Either a forward reference here, or move this\n     chapter after dissimilarity. Flagging rather than deciding. -->\n\n## Taxa across space and time\n\nOne site gives a history. Many sites give a geography as well. The Michigan download from @sec-neotoma2 holds many sites, and the same harmonisation\napplies to all of them.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmich_dl <- readRDS(\"./data/mich_dl.rds\")\n\nallSamp <- samples(mich_dl) %>%\n  dplyr::filter(ecologicalgroup %in% c(\"UPHE\", \"TRSH\"),\n                elementtype == \"pollen\",\n                units == \"NISP\") %>%      # the units filter matters, see @sec-neotoma2\n  mutate(variablename = replace(variablename,\n                                stringr::str_detect(variablename, \"Pinus.*\"),\n                                \"Pinus\"))\n\ndplyr::n_distinct(allSamp$siteid)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 29\n```\n\n\n:::\n:::\n\n\n### Where a taxon is present\n\nThe obvious first question is what fraction of sites record a taxon at a given time. Ages are\nbinned to the nearest 500 years, which is what `round(age * 2, -3) / 2` does.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntaxa_by_age <- allSamp %>%\n  dplyr::filter(variablename %in% focal, age < 11000) %>%\n  group_by(variablename, age_bin = round(age * 2, -3) / 2) %>%\n  summarise(n_sites_with_taxon = dplyr::n_distinct(siteid), .groups = \"drop\")\n\nsites_by_age <- allSamp %>%\n  dplyr::filter(age < 11000) %>%\n  group_by(age_bin = round(age * 2, -3) / 2) %>%\n  summarise(n_sites_total = dplyr::n_distinct(siteid), .groups = \"drop\")\n\npresence <- taxa_by_age %>%\n  inner_join(sites_by_age, by = \"age_bin\") %>%\n  mutate(proportion   = n_sites_with_taxon / n_sites_total,\n         variablename = factor(variablename, levels = focal))\n```\n:::\n\n\nBefore plotting it, look at what the numbers do:\n\n\n::: {.cell}\n\n```{.r .cell-code}\npresence %>%\n  group_by(variablename) %>%\n  summarise(bins = n(),\n            min_prop = min(proportion),\n            max_prop = max(proportion),\n            bins_at_100pct = sum(proportion == 1),\n            .groups = \"drop\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 6 × 5\n  variablename  bins min_prop max_prop bins_at_100pct\n  <fct>        <int>    <dbl>    <dbl>          <int>\n1 Quercus         23    1        1                 23\n2 Pinus           23    0.944    1                 22\n3 Picea           23    0.381    0.889              0\n4 Ulmus           23    0.944    1                 22\n5 Fraxinus        23    0.333    0.467              0\n6 Betula          23    0.889    1                 16\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# warning: false suppresses two \"iteration limit reached\" messages from mgcv. They are a\n# real symptom, not noise: a taxon present at 100% of sites in every bin is completely\n# separated, so the fit cannot converge. The table above shows the same thing in numbers.\nggplot(presence, aes(x = age_bin, y = proportion, weight = n_sites_total)) +\n  geom_point(size = 1) +\n  geom_smooth(method = \"gam\", formula = y ~ s(x),\n              method.args = list(family = \"binomial\")) +\n  facet_wrap(~variablename,\n             labeller = as_labeller(taxon_names)) +\n  scale_x_reverse(name = \"Age (cal years BP)\", labels = label_comma()) +\n  scale_y_continuous(name = \"Proportion of sites recording the taxon\",\n                     labels = label_percent()) +\n  theme_bw() +\n  theme(strip.text = element_marquee())\n```\n\n::: {.cell-output-display}\n![Proportion of Michigan sites recording each taxon through time, with a binomial GAM smooth weighted by the number of sites in each bin. Four of the six taxa are at or near 100% throughout, which is the problem described below.](visualisation_files/figure-html/fig-mich-presence-1.png){#fig-mich-presence width=672}\n:::\n:::\n\n\n*Quercus* is present at every site in every one of the 23 bins, and *Pinus* and *Ulmus* in 22 of\nthem. For those taxa the measure cannot go up or down, so the smooth is flat and tells you\nnothing. Presence is only informative for a taxon that is sometimes absent, which here is\n*Picea* and *Fraxinus*.\n\n<!-- NOTE (Claude): two things worth saying in prose here, both of which the code above already\n     demonstrates. First, `weight = n_sites_total` is not optional. Without it the binomial GAM\n     is fitted to bare proportions and mgcv warns \"non-integer #successes\" once per panel; the\n     old draft hid that with `warning: false`. Second, a taxon at 100% in every bin is complete\n     separation, which is why the fit struggles. This is the honest lead-in to the abundance\n     version below, which was your note in the old draft. -->\n\n### How abundant it is\n\nMean proportional abundance moves even when presence does not, so it is the more useful\nmeasure for common taxa. Proportions have to be calculated within each sample first, so that\nsites counted to different totals contribute equally.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmich_abund <- allSamp %>%\n  group_by(siteid, sampleid, age) %>%\n  mutate(prop = value / sum(value, na.rm = TRUE)) %>%   # within-sample proportions first\n  ungroup() %>%\n  dplyr::filter(variablename %in% focal, age < 11000) %>%\n  group_by(variablename, age_bin = round(age * 2, -3) / 2) %>%\n  summarise(mean_prop = mean(prop),\n            n_sites   = dplyr::n_distinct(siteid), .groups = \"drop\") %>%\n  mutate(variablename = factor(variablename, levels = focal))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mich_abund, aes(x = age_bin, y = mean_prop)) +\n  geom_line(colour = \"grey40\") +\n  geom_point(aes(size = n_sites), alpha = 0.6) +\n  scale_size_continuous(name = \"Sites\", range = c(0.4, 2.4)) +\n  facet_wrap(~variablename,\n             labeller = as_labeller(taxon_names)) +\n  scale_x_reverse(name = \"Age (cal years BP)\", labels = label_comma()) +\n  scale_y_continuous(name = \"Mean proportion of the pollen sum\",\n                     labels = label_percent(accuracy = 1)) +\n  theme_bw() +\n  theme(strip.text = element_marquee())\n```\n\n::: {.cell-output-display}\n![Mean proportion of the pollen sum for each taxon across Michigan sites, binned to 500 years. Point size is the number of sites contributing to each bin. Unlike @fig-mich-presence, every taxon varies.](visualisation_files/figure-html/fig-mich-abundance-1.png){#fig-mich-abundance width=672}\n:::\n:::\n\n\n<!-- NOTE (Claude): @fig-mich-abundance is where the ecology is, and the prose could point at the\n     Picea decline and the Quercus maximum. Note that the top-six-by-abundance selection drops\n     Tsuga, so the hemlock decline is no longer on the figure; add Tsuga to focal by hand if you\n     want to discuss it. -->\n\n<!-- NOTE (Claude): a further step, if you want one, is that binning to 500 years and taking an\n     unweighted mean treats a bin holding two sites the same as one holding twenty. Point size\n     shows the imbalance but does not correct it. That is a natural lead-in to @sec-gams. -->\n\n## Exercises\n\n::: {#exr-taxon-names}\n`taxon_names` is a dictionary of six entries, which is the right answer for six taxa. Find out\nwhere that stops being true.\n\n1. Get the full taxon list for Devil's Lake with\n   `sort(unique(devils_harm_prop$variablename))`. How many entries would a complete dictionary\n   need?\n2. Write the markdown for five of them that are **not** simple genus names. Neotoma records\n   families, `undiff.`, `-type` and `subg.` names, and they are not all italicised the same way.\n3. At what point would you stop writing entries by hand and write a rule instead? Say what the\n   rule would have to know, and what it would still get wrong.\n:::\n\n::: {#exr-stratplot}\nDraw a stratigraphic diagram for a site other than Devil's Lake, with taxa of your choosing,\nusing either `ggplot2` or `rioja`.\n\n1. Pick a site using the search methods in @sec-neotoma2 and download it with the gated pattern\n   in the respawn code above.\n2. Choose between five and eight taxa. Say why you chose those, given that the diagram is meant\n   to show how the site changed.\n3. Order the panels by weighted average age. Does the order match what you expected from the\n   ecology?\n:::\n\n::: {#exr-taxon-region}\nRepeat @fig-mich-abundance for a different state or region.\n\n1. Download the sites for your region, following @sec-neotoma2.\n2. Produce both the presence and the abundance version.\n3. Report how many of your taxa are present at 100% of sites in every bin. If the answer is most\n   of them, what does that tell you about the taxa you picked rather than about the region?\n:::\n\n\n::: {#exr-downsample}\n\n<!-- NOTE (Quinn): This may be quite a complex task... -->\n\nTake Devil's Lake, or another site of your choosing.\n\n1. Rebuild the chronology with `Bchron`, following @sec-age-depth, three times: once with the complete chronological control data, which is your reference point, and twice more with the control data artificially downsampled by removing rows. Keep the first and last control in each downsampled version.\n\n2. Artificially downsample the pollen data twice, again by removing rows. The complete dataset is your reference point here too.\n\n3. You now have three chronologies and three pollen datasets. Plot combinations of the two to see the effect of reduced temporal resolution in the chronological controls and in the pollen separately.\n:::\n\n## Conclusion\n\n<!-- NOTE (Claude): placeholder, yours to write. The three things the chapter actually\n     establishes are: a pollen diagram is a set of per-taxon panels against age with the oldest\n     at the bottom; anything drawn between two sampled levels is interpolation and should be\n     shown as such; and a measure that cannot vary, such as presence for a taxon found\n     everywhere, will produce a flat line that looks like a result. -->\n\nThe exercises are worth returning to if you skipped them:\n\n- @exr-freeaxis, what the `scales` argument does to a stratigraphic plot\n- @exr-taxon-names, formatting taxon names correctly\n- @exr-stratplot, a stratigraphic diagram of your own\n- @exr-taxon-region, presence against abundance in another region\n- @exr-downsample, what falls away as temporal resolution drops\n",
    "supporting": [
      "visualisation_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}